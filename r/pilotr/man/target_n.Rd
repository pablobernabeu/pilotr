% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/solve_curve.R
\name{target_n}
\alias{target_n}
\title{Solve a power curve for the sample size that reaches a target power}
\usage{
target_n(curve, target = 0.8, ...)
}
\arguments{
\item{curve}{A power curve, as returned by \code{\link[=power_curve_mixed]{power_curve_mixed()}} or by \code{\link[=sweep_spec]{sweep_spec()}} over
\code{units$subject$n}.}

\item{target}{The power to reach. Defaults to 0.8, the convention this package's plots draw a
line at.}

\item{...}{Further arguments passed to \code{\link[=solve_curve]{solve_curve()}}, such as \code{effect} to pick one focal
effect out of a curve holding several, or \code{level} for the interval.}
}
\value{
The list \code{\link[=solve_curve]{solve_curve()}} returns, with \code{n}, \code{n_lo} and \code{n_hi} added: \code{value}, \code{lo} and
\code{hi} rounded up to whole numbers.
}
\description{
The sample-size case of \code{\link[=solve_curve]{solve_curve()}}, and the number a power analysis is usually run to
obtain. Takes the curve a sweep over sample size has produced and returns the size at which
power reaches \code{target}, rounded up to a whole number of units alongside the exact solution.
}
\details{
Everything \code{\link[=solve_curve]{solve_curve()}} does applies here, including its refusals: a curve that never
reaches the target within the sizes it swept is refused rather than extrapolated, and the
reported interval can extend past the largest size simulated, which means the sweep was too
narrow to settle the question.

The whole-number fields round up rather than to nearest, because a design cannot recruit a
fraction of a subject and rounding down would leave the study short of the target it was
sized for.
}
\examples{
spec <- build_spec(list(name = "s", seed = 1, design_kind = "between", n_subject = 40,
  factor_name = "group", lev1 = "a", lev2 = "b", intercept = 0, effect = 0.7,
  family = "gaussian", resp_name = "score", sigma = 1))
# n_sims is small so the example runs quickly. Use 200 or more for real planning.
curve <- sweep_spec(spec, "units$subject$n", c(20, 40, 60, 80), power_design, n_sims = 50)
solved <- target_n(curve)
unlist(solved[c("n", "n_lo", "n_hi")])
}
\seealso{
\code{\link[=solve_curve]{solve_curve()}}, which this wraps, and \code{\link[=power_curve_mixed]{power_curve_mixed()}} for the curve.
}
