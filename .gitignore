# generated demo output
build/

# python
__pycache__/
*.pyc
.pytest_cache/
# pytest's --cov flags write these wherever the suite runs, and CI now always
# passes them, so a local run should not leave them staged for a commit
.coverage
coverage.json
*.egg-info/
dist/
.venv/
python/site/

# R
.Rhistory
.RData
*.Rproj.user

# R package build artifacts
r/pilotr/vignettes/*.knit.md
r/pilotr/vignettes/*.html
r/pilotr/vignettes/*_files/
r/pilotr/doc/
r/pilotr/Meta/
r/pilotr/docs/

# The published website. The site workflow assembles the landing page, the two
# documentation sites and the app into these two directories at the repository
# root, so anyone reproducing the build locally ends up with them in the tree.
/site/
/app-stage/


# Generated cross-language parity dumps; the harness is source, its output is not
tools/parity/out/
